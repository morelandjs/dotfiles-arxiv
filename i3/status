#
# ~/.config/i3status/config
#

# common settings for all hosts

general {
  interval = 10
  colors = true
  color_good = "#268bd2"
  color_degraded = "#b58900"
  color_bad = "#dc322f"
}

time {
  format = "%a %d %b %R"
}

load {
  format = "%1min %5min %15min"
}

order += "load"
order += "time"
